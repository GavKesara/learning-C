graphing in C is different to Cpp

C          |      C++
no Classes |    Supported clases

referance look ADTgraph.cpp 


method->

initalises a list array of size numVertices.  {ver1,ver2,ver3,...}
addEdge function pushes the new value into the vertice value using list pushing.

seems easy enough.

a list struct needs to be implemented which connects values across a vertice.
and the list values need to be included in a list array from which we need to access it.

looks like we dont necessarily need the list to go both ways.
review ADTlists 

In this part we will be using to store Graphs as in a vertice and its links.
for ex :-
    3           here the used addEdge cmds are as follows;
   /           
  0   1          addEdge(0,Graph,2) -> vertice 0 is linked to 2 | vertice 2 is linked to 0
   \ /           addEdge(0,Graph,3) -> vertice 0 is linked to 3 | vertice 3 is linked to 0
    2            addEdge(1,Graph,2) -> vertice 1 is linked to 2 | vertice 2 is linked to 1